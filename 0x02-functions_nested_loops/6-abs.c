#include "main.h"

/**
 * _abs - Computes the absolute value of an integer.
 * @i: input numbers as integers.
 *
 * Return: Absolute values.
 */

int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	return (-i);
}
