#include "main.h"


void puts2(char *str)
{
	int i = 0, x = 0, n;
	char *y = str;

	while (*y != '\0')
	{
		y++;
		i++;
	}

	x = i - 1;

	for (n = o; n <= x; n++)
	{
		if (o % 2 ==0)
			_putchar(str[o]);
	}
	_putchar('\n');
}

